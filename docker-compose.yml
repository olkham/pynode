services:
  pynode:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pynode
    ports:
      - "5000:5000"
    # Uncomment for mDNS support on Linux hosts (see DOCKER.md > mDNS /
    # Service Discovery). Not effective on Docker Desktop for Windows/macOS.
    # Remove the `ports:` mapping above when enabling this - it is ignored
    # in host networking mode.
    # network_mode: host
    volumes:
      # Mount persistent data
      - ./workflows:/app/workflows
      - ./logs:/app/logs
    environment:
      - PYTHONUNBUFFERED=1
      - HOST_IP=${HOST_IP:-}
    restart: unless-stopped
    # Rotate container logs so they cannot fill the host disk
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    deploy:
      resources:
        # Memory limit is a safety cap; tune to your hardware/models.
        limits:
          memory: 8g
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
