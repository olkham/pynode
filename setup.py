"""
PyNode - A Node-RED-like Visual Workflow System with Python Backend

This file is a shim kept for backwards compatibility (e.g. `python setup.py`
invocations). All package metadata and dependencies are declared in
pyproject.toml, which is the single source of truth.
"""

from setuptools import setup

setup()
