
TODO
- resolve sahi splitting / joining

MESSAGES
- check the inputs / outputs / arrays etc. are a standard form so node can be easily connected with little to no config
- allow variables to be set at msg level that can be used for the values in a node i.e.
    - msg.label = 'person' will use the label in the filter node
    - likewise the output message from nodes will contain the properties
    - msg.payload will contain the actual data
    